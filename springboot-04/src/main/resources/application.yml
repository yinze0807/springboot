server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shop?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  jpa:
    show-sql: true

mybatis:
  mapper-locations: classpath:/mapper/**

#全局主键自增
mybatis-plus:
  global-config:
    db-config:
      id-type: auto

